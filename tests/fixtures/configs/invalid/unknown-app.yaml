apps:
- id: salesforce
  vendor: Salesforce
  product: Lightning
  environments:
    dev:
      host: salesforce-dev.local
      base_path: /
  routes:
  - id: account-detail
    path: /lightning/r/Account/{id}/view
    pattern_type: path
    query_params: []
    server_visible: true
hosts:
- host: salesforce-dev.local
  service: harness-api
  app: no-such-app
  environment: dev
