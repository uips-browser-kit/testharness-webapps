app:
  id: servicenow
  vendor: ServiceNow
  product: Workspace
  environments:
    dev:
      host: servicenow-dev.local
      base_path: /
    prod:
      host: servicenow-prod.local
      base_path: /
  routes:
  - id: incident
    path: /now/workspace/agent/record/incident/{sys_id}
    pattern_type: path
    query_params: []
    server_visible: true
