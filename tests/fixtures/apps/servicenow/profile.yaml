app:
  id: servicenow
  vendor: ServiceNow
  product: Workspace
  environments:
    dev:
      host: instance.service-now.local
      base_path: /
    prod:
      host: servicedesk.company.local
      base_path: /
  routes:
  - id: incident
    path: /now/workspace/agent/record/incident/{sys_id}
    pattern_type: path
    query_params: []
    server_visible: true
